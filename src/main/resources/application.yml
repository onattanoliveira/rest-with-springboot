spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/udemy-rest-java-mysql?allowPublicKeyRetrieval=true&useSSL=false&useTimezone=true?serverTimezone=UTC
    username: root
    password: 2823656
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      show-sql: false
